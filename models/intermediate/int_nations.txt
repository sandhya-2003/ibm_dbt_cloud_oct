{{ config(materialized = 'incremental',
    unique_key='nation_id')
    
}}
with nation as (
    select 
    nation_id,
    region_id,
    name,
    updated_at
    from {{ ref("stg_nations") }}
)
select * from nation